@import "tailwindcss";

:root {
  --background: #ffffff;
  --foreground: #171717;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

@media (prefers-color-scheme: dark) {
  :root {
    --background: #0a0a0a;
    --foreground: #ededed;
  }
}

/* @media print {   body {     display: none !important;   } }*/

/* This helps hide content if someone tries to 'Print' to PDF to copy text */

body {
  background: var(--background);
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;
}

/* Add this at the end of globals.css */

@media print {
  @page {
    margin: 2cm;
  }
  
  /* Forces the background to be white and removes dark mode colors if active */
  body {
    background: white !important;
    color: black !important;
  }

  /* Hides navigation, footers, and any element with the 'no-print' class */
  header, 
  footer, 
  nav,
  .no-print {
    display: none !important;
  }

  /* Ensures the container takes up the full width of the paper */
  .print-container {
    width: 100% !important;
    border: none !important;
    shadow: none !important;
    margin: 0 !important;
    padding: 0 !important;
  }
}
